import 'package:flutter/material.dart';

class AppBorderRadius { 
  AppBorderRadius._(); 
  
  static const tr8tl8 = BorderRadius.only(
                          topLeft: Radius.circular(100),
                          topRight: Radius.circular(100),);

static BorderRadiusGeometry borderRadius10 = BorderRadius.circular(10);
 static BorderRadiusGeometry borderRadius8 = BorderRadius.circular(8);
  
  }